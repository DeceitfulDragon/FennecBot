const { randPlayerA } = require('../../commands/deathmatch.js');

//var randPlayerA;

module.exports = {
    start: [
        `${randPlayerA} Grabbed a backpack from The Heap, not realizing it was empty.`,
        `${randPlayerA} Ran away from The Heap and headed toward the forest to hide.`
    ]      
};